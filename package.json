{
  "name": "chatgpt-chatbot",
  "version": "1.0.0",
  "description": "This repository is for creating a chatbot using the GPT API. Users will need to have their own GPT key in order to be able to use this project.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.1.4",
    "express": "^4.18.2",
    "openai": "^3.2.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
